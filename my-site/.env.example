## Example env for local and production usage
# Copy this file to `.env` for local development or use it as a reference for CI/CD.

# API base used by the frontend. Set this to your backend base URL (omit trailing slash after /api if you like).
VITE_API_BASE="https://api.your-production-domain.com/api"

# For local development you may want:
# VITE_API_BASE="http://127.0.0.1:8000/api"

# IMPORTANT: Do not commit secret values. If you need private keys, store them in your CI or deployment platform.
